# Artivive Lite — Real AR Runtime

Android AR app using Google ARCore Augmented Images and SceneView. Create projects, choose a target image and video/image content, then scan targets with the camera.

The GitHub Actions workflow builds a debug APK automatically.
